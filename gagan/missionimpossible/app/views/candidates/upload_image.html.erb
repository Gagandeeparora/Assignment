<%= form_for :upload_file, :html => { :multipart => true }, method: :post do |f| %>
<div>
	<h1>Upload Picture</h1>
</div>

<%= f.hidden_field :candidate_id, :value => @candidate.id %>

<div class="field">
  <%= f.file_field :avatar %>
  <%= f.submit 'Upload' %>
</div>
<% end %>