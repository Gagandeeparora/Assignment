<h3> Comment </h3>

<%= form_for Interview.new, url: add_comment_path, method: :post do |f| %>

	<%= f.label :comment %><br>
	<%= f.text_area :comment %>

	<%= f.hidden_field :interview_id, :value => @interview.id %>


	<div class="actions">
    <%= f.submit "Save" %>
  </div>

<% end %>
